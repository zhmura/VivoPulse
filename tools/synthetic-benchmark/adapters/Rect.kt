package android.graphics

/** Metadata value only; the benchmark does not emulate images or ROI tracking. */
data class Rect(val left: Int = 0, val top: Int = 0, val right: Int = 0, val bottom: Int = 0)
