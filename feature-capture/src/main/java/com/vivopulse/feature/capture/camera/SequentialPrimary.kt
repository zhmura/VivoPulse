package com.vivopulse.feature.capture.camera

/**
 * Defines which channel should run first when the device only supports
 * sequential measurements (single camera at a time).
 */
enum class SequentialPrimary {
    FACE,
    FINGER
}


